spring:
  application:
    name: eureka
  cloud:
    config:
      uri: ${vcap.services.classified-config.credentials.uri:http://localhost:8888}
      label: develop

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://configserver:8888
      label: develop
  