info:
  component: Eureka - local

server:
  port: ${PORT:8761}

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    healthcheck: true
    
---
spring:
  profiles: eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka2.classified.cfapps.io/eureka/

---
spring:
  profiles: eureka2
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka.classified.cfapps.io/eureka/    
